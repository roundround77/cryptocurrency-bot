#include <iostream>
#include <string>
#include <vector>
#include <fstream>

/** function prototype*/

class cmdParser{
    public:
        static std::vector<std::string> parse(std::string input, char separator);
    private:
      

};